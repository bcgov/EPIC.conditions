// types.ts
export interface RouteMeta {
    title: string;
    path: string;
  }
  
  // RouteSegment for breadcrumb navigation
  export interface RouteSegment {
    title: string;
    path: string;
  }